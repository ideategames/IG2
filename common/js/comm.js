//
// dummy communication file
//

IGisIGServer = false
IGactiveComm = false

function DMMGetHttpRequest(packet,page) {
// this module communicates to the server database, thus is not made public
//
    return
}
